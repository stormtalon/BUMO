button(ng-click="$ctrl.setItem('Login Login With Your Spotify Account')") Login With Your Spotify Account
h5 BUMO is an app which shuffles Spotify tracks to create random playlists. Login with Spotify to listen to full length tracks.


div.buttonContainer
    button(ng-click="$ctrl.setItem('Combine Genres')") Combine Genres
    button(ng-click="$ctrl.setItem('Login to Save')") Login to Save
    button(ng-click="$ctrl.setItem('About Us')") About Us
